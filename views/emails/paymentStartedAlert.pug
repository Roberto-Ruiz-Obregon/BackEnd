extends baseEmail

block content
  p Hola #{firstName},
  p Hemos recibido tu información de pago para el curso: #{course.courseName}.
  p #{course.description}
  table.btn.btn-primary(role='presentation', border='0', cellpadding='0', cellspacing='0')
    tbody
      tr
        td(align='left')
          table(role='presentation', border='0', cellpadding='0', cellspacing='0')
            tbody
              tr
                td
                  img(src=`${course.imageUrl}`, style='width:100%')
  p Pronto revisaremos tu solicitud y nos pondremos en contacto contigo.